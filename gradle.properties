keycloakVersion=26.3.3
kotlinVersion=2.1.0
# Plugin version. Source of truth is package.json — bin/release.js syncs it
# here when `npm version` runs. Can also be overridden ad-hoc with -PartifactVersion=...
artifactVersion=1.0.0-rc.2
